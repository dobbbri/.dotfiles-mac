-- Pastas/arquivos ignorados pelo explorer (nvim-tree), busca (telescope)
-- e search & replace (grug-far). require("config.ignore") de onde precisar.
return { ".DS_Store", ".git", ".astro", "dist", "package-lock.json", "node_modules" }
